apiVersion: v1
kind: Service
metadata:
  name: {{ include "gromit-chart.fullname" . }}
  labels:
    {{- include "gromit-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gromit-chart.selectorLabels" . | nindent 4 }}
